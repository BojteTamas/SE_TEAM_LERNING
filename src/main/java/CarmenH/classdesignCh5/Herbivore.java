package CarmenH.classdesignCh5;

public interface Herbivore {
  // first example - we comment it
  public void eatPlants(); // seems that we can keep it

  // second example:
  public int eatPlants(int quantity);
}
