package CarmenH.classdesignCh5;

public class Whale {
    //private abstract void sing();
    // does not compile - it is private and not visible from child class
   // protected abstract void sing(); //this also doesn't work but because subclass cannot reduce the visibility of the parent method
}
