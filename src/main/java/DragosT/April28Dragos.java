package DragosT; /*
                 public class April28Dragos {
                 //Understanding Advanced Flow Control

                     int x = 20; while(x>0) {
                         do {
                             x -= 2
                         } while (x>5);
                         x--; System.out.print(x+"\t");
                     }
                 //    On the second iteration of the outer loop, the inner do-while will
                 //    be executed once, even though x is already not greater than 5. As you may recall,
                 //    do-while statements always execute the body at least once. This will reduce the value to 1, which will be further
                 //    lowered by the decrement operator in the outer loop to 0. Once the value reaches 0, the outer loop will terminate.
                 //    The result is that the code will output the following: 3  0

                 int[][] myComplexArray = {{5,2,1,3},{3,9,8,9},{5,7,12,7}};
                     OUTER_LOOP: for(int[] mySimpleArray : myComplexArray) {
                         INNER_LOOP: for(int i=0; i<mySimpleArray.length; i++) {
                             System.out.print(mySimpleArray[i]+"\t");
                         }
                         System.out.println();
                     }
                 // Optional labels are often only used in loop structures. While this topic is not on the OCA exam, it is possible to
                 // add optional labels to control and block structures, it is rarely considered good coding practice to do so.
                 }

                  */
