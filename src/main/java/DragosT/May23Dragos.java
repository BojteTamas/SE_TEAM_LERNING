package DragosT; /*
                 public class May23Dragos {
                 //using and array
                 // elements are indexed starting with 0.
                 String[] birds = new String[6];
                 System.out.println(birds.length); // 6 -> all 6 elements of the array are null

                 //Sorting (import java.util.Arrays;) to use sort function
                 int[] numbers = { 6, 9, 1 };
                 Arrays.sort(numbers);
                 for (int i = 0; i < numbers.length; i++)
                 L System.out.print (numbers[i] + " "); // prints 1 6 9 loop required to print values not hash
                 // sort for strings
                 String[] strings = { "10", "9", "100" };
                 Arrays.sort(strings);
                 for (String string : strings)
                 System.out.print(string + " "); // will sort alphabetically so 10 100 9 since 1 is before 9

                 // Searching an array (binarySearch requires the array to be sorted)
                 int[] numbers = {2,4,6,8};
                 System.out.println(Arrays.binarySearch(numbers, 2)); // 0 index
                 System.out.println(Arrays.binarySearch(numbers, 4)); // 1 index
                 System.out.println(Arrays.binarySearch(numbers, 1)); // -1 index ->java knows it's position should be before 2 at index 0 so it sbustracts 1 from 0
                 System.out.println(Arrays.binarySearch(numbers, 3)); // -2 index
                 System.out.println(Arrays.binarySearch(numbers, 9)); // -5 index


                 }

                  */
