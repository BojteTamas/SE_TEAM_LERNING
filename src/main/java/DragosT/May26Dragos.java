package DragosT; /*public class May26Dragos {
                 //wrapper clases Integer.valueOf is the wrapper
                 int primitive = Integer.parseInt("123");
                 Integer wrapper = Integer.valueOf("123");

                 //Converting list to array
                 List<String> list = new ArrayList<>();
                 list.add("hawk");
                 list.add("robin");
                 Object[] objectArray = list.toArray();
                 System.out.println(objectArray.length); // is 2
                 String[] stringArray = list.toArray(new String[0]);
                 System.out.println(stringArray.length); // 2

                 //converting array to list
                 String[] array = { "hawk", "robin" }; // [hawk, robin]
                 List<String> list = Arrays.asList(array); // returns fixed size list
                 System.out.println(list.size()); // is 2
                 list.set(1, "test");  // new list [hawk, test]
                 array[0] = "new"; // new list [new, test]
                 for (String b : array) System.out.print(b + " "); //new test
                 list.remove(1); // throws error, changing list size is not allowed

                 }

                  */
