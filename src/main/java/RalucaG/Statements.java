package RalucaG;

public class Statements {
  public static void main(String[] args) throws Exception {
    /**
     * BufferedReader reader = new BufferedReader(new InputStreamReader(System.in)); int Weekday =
     * Integer.parseInt(reader.readLine()); String dayDisplay = "No such day of the week";
     *
     * <p>if (Weekday == 1) { dayDisplay = "Monday"; } if (Weekday == 2) { dayDisplay = "Tuesday"; }
     * if (Weekday == 3) { dayDisplay = "Wednesday"; } if (Weekday == 4) { dayDisplay = "Thursday";
     * } if (Weekday == 5) { dayDisplay = "Friday"; } if (Weekday == 6) { dayDisplay = "Saturday"; }
     * if (Weekday == 7) { dayDisplay = "Sunday"; }
     *
     * <p>System.out.println(dayDisplay); }
     *
     * <p>BufferedReader reader = new BufferedReader(new InputStreamReader(System.in)); // read
     * number from keyboard int number = Integer.parseInt(reader.readLine()); //parse number
     *
     * <p>if (number > 0) { number = number * 2; } else if (number < 0) { number = number + 1; }
     * System.out.println(number);
     */
  }
}
